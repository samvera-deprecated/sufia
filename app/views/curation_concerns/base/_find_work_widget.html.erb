<%= f.input :ordered_member_ids,
      label: false,
      type: :text,
      input_html:{
        id: id_name,
        name: f.object.model_name.param_key + '[ordered_member_ids][]',
        :'data-autocomplete-url' => Rails.application.routes.url_helpers.qa_path + '/search/local/works',
        :'data-autocomplete' => 'work',
        :'data-user' => user_email,
        :'data-id' => id,
        autocomplete: 'off',
        :'aria-labelledby' => id_name + '_label',
        :class => 'new-form-control string multi_value optional related_works_ids form-control multi-text-field ui-autocomplete-input'
      }
 %>
